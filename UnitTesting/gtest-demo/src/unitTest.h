#pragma once
using namespace std;
#include <string>

double randomNumber(double prob);

string getState(string);

string chooseArmyType(int);

string chooseSoldierRank(int);

string chooseVehicleType(int);